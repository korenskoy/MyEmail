// Single source of truth for app version.
// MARKETING_VERSION — user-facing (CFBundleShortVersionString). Bump by hand for releases.
// CURRENT_PROJECT_VERSION — build number (CFBundleVersion). Auto-incremented by scripts/build-dmg.sh.

MARKETING_VERSION = 2.1
CURRENT_PROJECT_VERSION = 170
